#include<iostream>
using namespace std;
int main()
{
    int a,b,c;
    cout<<"Enter three no.s:";
    cin>>a>>b>>c;
    if (a>b)
        if(a>c){
            cout<<"max a:";}

        else
        {
            cout<<"max c:";
        }
    
    else if(b>a)
        if(b>c)
        {
            cout<<"max b:";
        }
        else
        {
            cout<<"max c:";
        }
    else 
    cout<<"max c";
    }
